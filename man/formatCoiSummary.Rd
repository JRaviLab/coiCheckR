% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_lists.R
\name{formatCoiSummary}
\alias{formatCoiSummary}
\title{Format a batch COI screen as a readable text list}
\usage{
formatCoiSummary(batch)
}
\arguments{
\item{batch}{Output of \code{\link[=checkCoiBatch]{checkCoiBatch()}}.}
}
\value{
A character vector, one line per candidate, e.g.
\code{"Smith AB -- 1 direct (Lee C), 0 second-degree, 0 funding [potential_conflict]"}.
A line ends with \code{"(sources failed: ...)"} if any evidence source
errored, since that's when a candidate could look clear only
because part of the search didn't run.
}
\description{
Turns \code{\link[=checkCoiBatch]{checkCoiBatch()}}'s output into plain-text lines suitable
for pasting into an email to an editor -- the kind of summary this
package's design doc describes as "evidence, not a verdict": each
line states what was found, not a conflicted/clear judgment.
}
\examples{
batch <- list(
  `Smith AB` = structure(
    list(
      candidate = "Smith AB",
      authors = "Lee C",
      direct = tibble::tibble(
        candidate = "Smith AB", author = "Lee C", PMID = "1", year = 2020L
      ),
      second_degree = tibble::tibble(),
      funding = tibble::tibble()
    ),
    class = "coiReport"
  ),
  summary = tibble::tibble(
    candidate = "Smith AB",
    n_direct = 1L, n_second_degree = 0L, n_funding = 0L
  )
)
formatCoiSummary(batch)
}
