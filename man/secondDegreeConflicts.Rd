% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coauthor_network.R
\name{secondDegreeConflicts}
\alias{secondDegreeConflicts}
\title{Detect second-degree co-authorship conflicts}
\usage{
secondDegreeConflicts(
  candidate_name,
  author_names,
  affiliation = NULL,
  min_year = NULL,
  min_shared_pubs = 2
)
}
\arguments{
\item{candidate_name, author_names}{As in \code{\link[=reporterSharedAwards]{reporterSharedAwards()}}.}

\item{affiliation}{Optional affiliation substring to disambiguate the
\emph{candidate} when their surname is common (passed to
\code{\link[=pmSearchAuthor]{pmSearchAuthor()}}, same as \code{\link[=checkCoi]{checkCoi()}}'s \code{affiliation} argument).
Without this, a common-surname candidate's second-degree results can
include an unrelated same-named person's collaborator network.}

\item{min_year}{Restrict co-authorship evidence to this year or later
(e.g. \code{Sys.Date() |> format("\%Y") |> as.integer() - 4} for a 4-year
window matching common COI policy).}

\item{min_shared_pubs}{Minimum number of shared PMIDs between the
candidate's collaborator and the candidate for that collaborator to
count as a "frequent" one. Default 2 (i.e. not a single one-off
co-authorship).}
}
\value{
A tibble: \code{candidate_collaborator}, \code{linked_author},
\code{n_shared_with_candidate}, \code{evidence_pmid} (the PMID linking the
collaborator to the author).
}
\description{
A second-degree conflict exists when the \emph{candidate} has not
published directly with an \emph{author}, but the candidate's frequent
collaborator has -- i.e. there is a length-2 path in the co-authorship
graph. This is a soft signal for editorial judgment, not an automatic
disqualifier (per the RAP / knowledge-graph COI literature this
package's design note is based on): a candidate's one-off co-author
three years ago being tied to a paper author is much weaker evidence
than a recurring collaborator.
}
\examples{
tryCatch(
  secondDegreeConflicts("Smith AB", "Lee C", min_year = 2020),
  error = function(e) message("Live PubMed API unavailable: ", conditionMessage(e))
)
}
