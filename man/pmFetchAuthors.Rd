% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pubmed.R
\name{pmFetchAuthors}
\alias{pmFetchAuthors}
\title{Fetch author lists and metadata for a set of PMIDs}
\usage{
pmFetchAuthors(PMIDs, batch_size = 150, pause = 0.4)
}
\arguments{
\item{PMIDs}{Character or integer vector of PMIDs.}

\item{batch_size}{Records per \code{efetch} call. Default 150 (safely under
the unauthenticated rate cap; raise if you set an NCBI API key via
\code{rentrez::set_entrez_key()}).}

\item{pause}{Seconds to sleep between batches, to stay within NCBI's
rate limits (3 req/sec without a key, 10 req/sec with one).}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per (PMID, author): columns
\code{PMID}, \code{year}, \code{journal}, \code{author_last}, \code{author_fore},
\code{affiliation}.
}
\description{
Retrieves PubMed XML records in batches (E-utilities caps \code{efetch} at
~200 IDs per call for unauthenticated use; this function chunks
automatically) and parses each record's author list, publication year,
and journal.
}
\examples{
\donttest{
# Live PubMed calls -- \donttest for the same reason as
# pmSearchAuthor()'s example.
tryCatch(
  {
    ids <- pmSearchAuthor("Smith AB")
    pmFetchAuthors(ids[seq_len(min(5, length(ids)))])
  },
  error = function(e) message("Live PubMed API unavailable: ", conditionMessage(e))
)
}
}
