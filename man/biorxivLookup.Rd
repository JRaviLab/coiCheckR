% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biorxiv.R
\name{biorxivLookup}
\alias{biorxivLookup}
\title{Look up a bioRxiv (or medRxiv) preprint's author list by DOI}
\usage{
biorxivLookup(doi, server = c("biorxiv", "medrxiv"))
}
\arguments{
\item{doi}{Character scalar, e.g. \code{"10.1101/2020.05.17.095000"}.}

\item{server}{One of \code{"biorxiv"} or \code{"medrxiv"}.}
}
\value{
A one-row tibble with \code{doi}, \code{title}, \code{authors} (single
semicolon-delimited string, as returned by the API), \code{date},
\code{category}, \code{published} (linked journal DOI once formally
published, or \code{NA}). Returns zero rows if the DOI is not found.
}
\description{
The public bioRxiv API (\url{https://api.biorxiv.org}) has no author- or
name-search endpoint -- it only resolves metadata for a DOI you
already have. Use this to \emph{enrich} a preprint you already found (e.g.
via PubMed's "preprint" linkouts, or a DOI cited in the manuscript's
bibliography), not to discover a candidate's preprints from scratch.
For preprint discovery, PubMed itself now indexes many bioRxiv/medRxiv
records directly, so \code{\link[=pmSearchAuthor]{pmSearchAuthor()}} is often sufficient.
}
\examples{
tryCatch(
  biorxivLookup("10.1101/2020.05.17.095000"),
  error = function(e) message("bioRxiv API unavailable: ", conditionMessage(e))
)
}
