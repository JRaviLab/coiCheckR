% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_viz.R
\name{coiToGraph}
\alias{coiToGraph}
\title{Build an igraph object from one or more COI reports}
\usage{
coiToGraph(report)
}
\arguments{
\item{report}{A \code{coiReport} object, or a (possibly named) list of
them.}
}
\value{
An \code{\link[igraph:graph_from_data_frame]{igraph::graph_from_data_frame()}} object. Node attribute
\code{type} is \code{"candidate"} or \code{"author"}; edge attribute \code{relation}
is \code{"direct"}, \code{"second_degree"}, or \code{"funding"}; edge attribute
\code{evidence} holds the PMID/award number/collaborator name backing
it.
}
\description{
Turns \code{coiReport} evidence into a graph: one node per candidate and
per author, one edge per piece of evidence (direct co-authorship,
second-degree co-authorship, or shared funding), edge-typed
accordingly. Accepts either a single \code{\link[=checkCoi]{checkCoi()}} result or the
list returned by \code{\link[=checkCoiBatch]{checkCoiBatch()}} (its \code{summary} element, if
present, is ignored).
}
\examples{
report <- structure(
  list(
    candidate = "Smith AB",
    authors = "Lee C",
    direct = tibble::tibble(
      candidate = "Smith AB", author = "Lee C", PMID = "1", year = 2020L
    ),
    second_degree = tibble::tibble(),
    funding = tibble::tibble()
  ),
  class = "coiReport"
)
coiToGraph(report)
}
